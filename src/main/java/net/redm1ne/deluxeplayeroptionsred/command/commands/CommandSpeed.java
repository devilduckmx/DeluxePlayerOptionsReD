package net.redm1ne.deluxeplayeroptionsred.command.commands;

import net.redm1ne.deluxeplayeroptionsred.DeluxePlayerOptions;

/**
 * Command to toggle speed mode.
 */
public class CommandSpeed extends AbstractOptionCommand {

    public CommandSpeed(DeluxePlayerOptions plugin) {
        super(plugin, "speed");
    }
}
