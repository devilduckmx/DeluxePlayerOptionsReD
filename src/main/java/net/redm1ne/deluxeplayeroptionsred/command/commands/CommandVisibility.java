package net.redm1ne.deluxeplayeroptionsred.command.commands;

import net.redm1ne.deluxeplayeroptionsred.DeluxePlayerOptions;

/**
 * Command to toggle player visibility.
 */
public class CommandVisibility extends AbstractOptionCommand {

    public CommandVisibility(DeluxePlayerOptions plugin) {
        super(plugin, "visibility");
    }
}
